{
  "name": "information-storage-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network",
    "verify": "hardhat run scripts/verify.ts --network",
    "coverage": "hardhat coverage",
    "gas": "hardhat test --gas-report"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@typechain/ethers-v6": "^0.4.0",
    "@typechain/hardhat": "^8.0.0",
    "hardhat": "^2.12.0",
    "hardhat-gas-reporter": "^1.0.9",
    "typechain": "^8.0.0"
  }
}

{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "generate-types": "typechain --target=ethers-v5 --out-dir=src/abis/types 'src/abis/*.json'"
  },
  "dependencies": {
    "@web3modal/ethers": "^2.0.0",
    "ethers": "^6.0.0",
    "ipfs-http-client": "^60.0.0",
    "next": "14.0.0",
    "react": "^18.0.0",
    "zustand": "^4.0.0"
  }
}
